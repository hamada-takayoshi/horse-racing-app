# AGENTS.md

## 開発ルール

### 資料の正本

- プロジェクト概要とセットアップ手順は`README.md`を参照する。
- アプリケーション方式・構成は`docs/application/00_architecture.md`を正本とする。
- DB設計の正本と参照順は`docs/database/README.md`に従う。
- 仕様を変更した場合は、実装と対応する正本資料を同じ変更で更新する。

### 秘密情報と安全な操作

- `.env`、パスワード、接続文字列などの秘密情報をソース、ログ、コミットへ含めない。
- アプリケーションはDB専用ユーザーで接続し、`sa`を使用しない。
- アプリケーションの待受先はloopbackに限定する。LANやインターネットへの公開は、認証・通信保護などを設計したうえで明示的に依頼された場合に限る。
- `database/scripts/reset-db.ps1`はDBデータを削除するため、ユーザーの明示的な依頼なしに実行しない。

### アプリケーションとDBの実装

- BlazorコンポーネントにSQLアクセスや業務ルールを直接記述せず、アプリケーションサービスとデータアクセス処理へ分離する。
- SQLは必ずパラメーター化し、利用者入力を文字列連結で埋め込まない。
- DBスキーマの正本は`database/ddl/`とDB設計資料とする。ORMマイグレーションで独自にスキーマを変更しない。
- DDLを変更した場合は`database/scripts/initialize-db.ps1`と`database/scripts/verify-db.ps1`で検証する。アプリケーション実装後は、変更に応じてビルドとテストを実行する。

### PowerShellスクリプトの文字コード

- `*.ps1`ファイルはUTF-8 BOM付きで保存する。
- 日本語を含むスクリプトをWindows PowerShellで実行した際の文字化けを避けるため、編集や生成時にもBOMを維持する。
- スクリプトの文字コードや改行コードを変換する場合は、変換後の文字コードを明示して確認する。